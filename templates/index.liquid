<h2>{{ collections['home-page'].title}}</h2>
{% for product in collections['home-page'].products %}
  <div>
    <a href="{{ product.url }}" >
      <img src="{{ product.featured_image | image_url: width: 300 }}" />
      <h3>{{product.title}}</h3>
      <p>{{product.price | money }}</p>
    </a>
  </div>k
  {{- product.title | link_to: product.url }}
{% endfor %}